extends layout

block content
  .total
    #all
      #h1
        h1 #{title}
      p #{hint}
      #form
        form(method="post")
          #name
            用户名：
            input(type="text", name="username")
          div
            密码：
            input(type="password", name="password")
          .signin
            input(type="submit", value="登录", method="post", class="button")
          button.button
            a(href="/regist") 注册